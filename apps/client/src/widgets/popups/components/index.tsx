import DetailsPopup from './Details/Details';
import NotificationPopup from './Notification/Notification';
import PickerPopup from './Picker/Picker';

const index = () => {
  return (
    <div>
      <DetailsPopup />
      <NotificationPopup />
      <PickerPopup />
    </div>
  );
};

export default index;
